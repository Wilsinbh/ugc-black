# W-1

Generated by comfyui-wizard. Submission status: **build_failed**.

## Build

```bash
docker build -t comfyui-wizard/w-1:latest . \
  --build-arg HF_TOKEN=$HF_TOKEN
```

## Run

```bash
docker run --rm --gpus all -p 8188:8188 comfyui-wizard/w-1:latest
```

Open http://localhost:8188

## Files

- `Dockerfile` — build recipe
- `workflow.json` — original workflow as submitted
- `api-workflow.json` — API-format workflow (if convertible)
- `analysis.json` — full resolution result

Re-fetch the latest version any time at `https://comfy.getrunpod.io/api/submissions/w-1/bundle`.
